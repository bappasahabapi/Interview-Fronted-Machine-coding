function addsum(n1: number, n2: number): number {
    return (n1 + n2);
}

let ans = addsum(5, 6);
console.log(ans)

const add = (a: number, b: number): number => {
    let sum = a + b;
    return sum;
}

let test = add(5, 3);
console.log(test)